[project]
name = "openclaw-governance-proxy"
version = "0.1.0"
description = "Local-first HTTPS governance proxy for OpenClaw and AI agents"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
